export class Enemy {
    constructor(name, health, maxHealth, strength) {
        this.name = name;
        this.health = health;
        this.maxHealth = maxHealth;
        this.strength = strength;
    }
}