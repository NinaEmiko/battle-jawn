// import { render, screen } from '@testing-library/react';
// import '@testing-library/jest-dom/extend-expect';
// import HowToArena from './HowToArena';

// test('renders the component with correct content and class names', () => {
//   render(<HowToArena />);

//   const divElement = screen.getByRole('generic', { name: /child-jawn/i });
//   expect(divElement).toBeInTheDocument();
//   expect(divElement).toHaveClass('child-jawn');

//   const paragraphElement = screen.getByText(/Enter arena to fight enemies./i);
//   expect(paragraphElement).toBeInTheDocument();
//   expect(paragraphElement).toHaveClass('text-jawn');
// });
