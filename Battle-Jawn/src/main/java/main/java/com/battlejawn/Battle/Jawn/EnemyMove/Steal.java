package main.java.com.battlejawn.Battle.Jawn.EnemyMove;

public class Steal {

    private boolean success;

    // If user is DPS, enemy attacks instead of stealing
    //

}

// let successRate = Math.floor(Math.random() * 100);
// if (user.role === 'DPS') {
// enemyStab();
// } else {
// if (user.potions > 0) {
// if (successRate > 90){
// createEnemy.potions++;
// user.potions--;
// logBox.push(`${createEnemy.name} stole a potion!`);
// setTimeout(userInterface, 1000);
// setTimeout(logBoxDisplay, 1000);
// } else {
// logBox.push(`${createEnemy.name} attempted to steal! Attempt was
// unsuccessful!`);
// setTimeout(userInterface, 1000);
// setTimeout(logBoxDisplay, 1000);
// }
// } else {
// logBox.push(`${createEnemy.name} attempted to steal! Too bad you're a broke
// bitch.`);
// setTimeout(userInterface, 1000);
// setTimeout(logBoxDisplay, 1000);
// }
